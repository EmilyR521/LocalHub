/**
 * Gardener plugin: plant data is stored via plugin store (plants, zones, jobs, etc.).
 * No external API; users add their own plant details and can import from CSV.
 */
import { Router } from 'express';

const router = Router();

export default router;
